version: '3.1'

services:

  mongo:
    image: mongo:6.0.1
    restart: always
    ports:
      - 27017:27017
    environment:
      MONGO_INITDB_ROOT_USERNAME: dlike-dba
      MONGO_INITDB_ROOT_PASSWORD: motopia